/** Target Chip-8 instructions per second (typical range ~500–1000) */
export const CHIP8_SPEED = 700;

/** Display / timer refresh rate in Hz */
export const CHIP8_TIMER_HZ = 60;

/** Default beep frequency in Hz while the sound timer is active */
export const CHIP8_BEEP_FREQUENCY = 440;
